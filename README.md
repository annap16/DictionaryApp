# Dictionary Application

This is a Polish-to-English dictionary application with a client-server architecture. The server handles database interactions and translations, while the client provides a command-line interface for users.

## Features
- Add, modify, and delete words and translations
- Store example sentences for translations
- Query translations using a simple CLI
- GraphQL-based API for database interactions
- Dockerized setup for easy deployment

